<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@+id/MainFragment">

    <fragment
        android:id="@+id/MainFragment"
        android:name="ge.edison.lockers.MainFragment"
        android:label="Main"
        tools:layout="@layout/fragment_main">
        <action
            android:id="@+id/action_MainFragment_to_errorDisplay"
            app:destination="@id/errorDisplay" />
    </fragment>
    <activity
        android:id="@+id/errorDisplay"
        android:name="ge.edison.lockers.ErrorDisplay"
        android:label="ErrorDisplay" />
</navigation>